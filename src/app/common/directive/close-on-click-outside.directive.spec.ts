import { CloseOnClickOutsideDirective } from './close-on-click-outside.directive';

describe('CloseOnClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
